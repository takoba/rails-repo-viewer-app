<%= form_with(model: saved_issue, local: true) do |form| %>
  <% if saved_issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(saved_issue.errors.count, "error") %> prohibited this saved_issue from being saved:</h2>

      <ul>
      <% saved_issue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name %>
  </div>

  <div class="field">
    <%= form.label :repo_name %>
    <%= form.text_field :repo_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
